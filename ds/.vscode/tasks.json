{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-run",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-I", "${workspaceFolder}/include",
                "-I", "${workspaceFolder}/include/*",
                "${file}",
                "${workspaceFolder}/src/**/*.c",
                "-o", "${fileDirname}/${fileBasenameNoExtension}",
                "-g"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Build and run files for any data structure in the project."
        }
    ]
}